/**
 * This is a generated sub-class of _Authentication.as and is intended for behavior
 * customization.  This class is only generated when there is no file already present
 * at its target location.  Thus custom behavior that you add here will survive regeneration
 * of the super-class. 
 **/
 
package services.authentication
{

	public class Authentication extends _Super_Authentication
	{
		// Constructor
		public function Authentication( hostname:String )
		{
			super( hostname );
		}
              
	}

}
